---
name: ppt-flair
description: 高层汇报/大会/展会「金句页」视觉设计工作流（封面与页内金句页均可）。当用户需要"PPT汇报风格"、"总经理大会封面"、"书法字标题"、"高层汇报底版"、"金句页"、"领导汇报视觉"、"大会汇报封面"、"高级商务视觉"等高管级演示视觉时使用。核心流程六阶段：需求理解+自动选书风(5种书法风格库)→AI书法标题生成→气质底图筛选(素材库检索+AI生成)→底图精修(脚本调色)→背景蒙版系统→精调循环。**标准交付（2026-08 定稿）：至少 3 种书风 × 至少 2 张底图，组合产出 6 张视觉候选**，HTML 画廊一次交付。内置书法风格参考库(5种：狂野榜书/东方诗意/清新文艺/设计手写/狂草奔放)、风格清单与配色/蒙版配方、可执行工具链脚本。风格基准：深色底+低饱和叠加+书法大字+强对比+单视觉焦点。agent_created: true
---

# PPT Executive Visual（高层汇报视觉 · 封面与金句页）

## Overview

为高层汇报、总经理大会、展会宣讲等场景提供一套可复现的高端视觉设计工作流，产出物统称「金句页」——既包括封面，也包括正文中承载核心观点/口号/价值观的整幅页面（如展会背景板、章节金句页、尾页宣言）。核心理念是"书法字 + 气质底图 + 蒙版校色"三层结构，以深色基调营造克制、高级、强对比的视觉语言。本 skill 是风格系统而非通用 PPT 教程——所有阶段均围绕"高管可读性 + 高端视觉节奏"这一标准执行。

风格基准可参考 `assets/examples/` 下 5 张真实交付案例图。

## 触发词

- "PPT汇报风格" / "高层汇报PPT风格"
- "总经理大会封面" / "大会汇报封面"
- "书法字标题" / "领导汇报视觉"
- "高层汇报底版" / "蒙版设计" / "金句页"
- 任何要求"高端/大气/商务严肃"的演示封面/金句页需求

## 工作流（6 阶段）

### Stage 0：需求理解 + 自动选书风

1. **接收用户输入**：标题文案 + 场合（大会/汇报/战略发布等）+ 情绪基调（如"突破"、"稳健"、"磅礴"）。若用户未提供，用提问引导补全。
2. **AI 自动选书风**：根据标题语义 + 场合 + 情绪，查询 `references/calligraphy-style-ref.md` 中的「自动选书风匹配矩阵」，选出 **3 种推荐书风**（按匹配度排序，用户可增减）。
   - 情绪关键词优先匹配：磅礴/霸气 → 狂野榜书；传承/底蕴 → 东方诗意；创新/新锐 → 清新文艺；设计/灵动 → 设计手写；冲刺/决战 → 狂草奔放；沉稳/权威 → 东方诗意。
   - 字数适配：2-4 字全部适用；5-6 字避开狂野榜书和狂草奔放（易杂乱）；7 字以上优先东方诗意/设计手写。
3. **向用户展示推荐书风**：列出 3 种风格名 + 对应情绪 + 视觉特征摘要，让用户确认或调整。
4. 输出物：确认后的 3 种目标书风列表。

### Stage A：标题字体概念（AI 自动生成候选）

1. 根据 Stage 0 确认的书风，每种书风各生成 1 张 AI 书法候选。
2. **首选路径（已实测跑通）**：用内置 AI 生图生成书法字——从 `references/calligraphy-style-ref.md` 中提取该风格的标准生图提示词，替换为当前标题文案，生成黑底白字书法图。
   - **风格锚定（关键技巧，2026-08 实测教训）**：纯文字提示词对"书风气质"的区分度极低——三种不同风格提示词曾生成几乎一样的图。必须用以下手段锚定风格：
     a. **书体名词硬锚点**：提示词里写明具体书体（楷书/隶书/行书/草书/榜书/硬笔手写体），抽象气质词（"飘逸""灵动"）只做辅助描述；
     b. **图生图参考**：把 `assets/calligraphy-ref/` 中对应风格的基准图作为 image-to-image 输入（指令写"用这种笔触风格书写：标题文案"），让模型直接学参考图的笔触气质。
3. **风格多样性硬关卡（2026-08 用户明确要求）**：交付的候选中，**至少 3 种视觉上明显可区分的书风**（笔画粗细、结构张力、连带程度粗看即可分辨）。生成后先自检：任意两张若粗看雷同，必须换书体锚点/换参考图重生成，不允许"伪多风格"交付。
4. **亮度抠图**：AI 出图为黑底白字，用 `scripts/keyout_title.py` 按亮度转透明 PNG（白字保留、黑底剔除）。
5. **水印清理（2026-08 实测必做）**：AI 生图右下角带"AI生成"水印，抠图后随白字保留——合成前必须把标题图右下角约 180×60px 区域 alpha 置 0；底图水印同理，用上方相邻像素修补约 200×60px 区域。
6. **错字检查是硬关卡**：AI 生成中文书法有错字风险，透明 PNG 合成前必须让用户确认每个字的字形，有错字就换提示词重生成（一般 1-2 次内命中）。
7. **备选路径**：系统字体库直接渲染（华文行楷/新魏/琥珀），零错字但笔画"印刷感"平，质感明显不如 AI 生图，仅在用户拒绝 AI 路径时使用。
8. 输出物：**3 张透明 PNG 标题候选（3 种明显不同书风）**，每个标题的情绪说明。

---
> **交付节奏（2026-08 用户定稿）**：标准模式为**一次跑完**——Stage A 出 3 种书风、Stage B 出 ≥2 张底图，Stage E 直接组合出 6 张完整视觉，HTML 画廊一次交付，用户在 6 张成品里挑。逐阶段确认仅用于用户明确要求"一步步来"的场景。

### Stage B：底图生成（纯 AI 生成，双风格交付）

1. **唯一路径：AI 生成（2026-08 用户定稿）**——底图不再使用素材库图片，一律 AI 生成。原因：① 版权干净（自生成，可公开分发）② 质感可控（可反复重生成）③ 与需求主题定制匹配。
   - 提示词关键词：电影级/史诗级、大气恢弘、光占大面积画面、画面下部保留暗部给画面主体、低饱和、无文字无水印。
   - **核心原则：避免"过度压抑"**——纯暗调小光比会被用户否掉；要"大气"就得拉开尺度：暖金晨光做主光、深蓝做基底、云海/山脉撑开空间感。
   - 使用 style-checklist.md 中的提示词模板（已含正反例），按需求方向出候选。
   - 按以下标准逐项打分，四项全过才入选：氛围匹配、构图干净、对比度强、低噪点。
   - **注意**：AI 生成底图右下角有"AI生成"水印，合成前需用上方相邻像素修补右下角约 200×60px 区域。
   - 素材库（`assets/backgrounds/`）仅作**创意方向参考**（翻看找灵感），不得直接作为底图输出。

2. **风格多样性硬关卡（2026-08 用户明确要求）**：底图候选**至少 2 张、覆盖至少 2 种不同风格方向**（如"金色光河" vs "晨曦山巅" vs "云海帆船"），不允许同风格微调凑数。每张底图需对应标题语义的不同侧面。

3. **输出物**：AI 生成底图 ≥2 张（不同风格方向）+ 入选理由。

### Stage C：底图精修

1. **首选路径（已实测跑通）**：用 Python PIL 脚本做裁剪与调色，全流程可复现、参数可记录。
2. 裁剪：按 1920×1080（16:9）中心裁切，PPT 全幅直接用。
3. 若底图需要多张合成（如天空+山体），先统一色温再叠加。
4. 输出物：1 张精修主图（高清，预留标题区）。

### Stage D：背景系统搭建

1. **实测定稿配方（2026-08，用户逐轮确认，直接复用）**：
   - 饱和度 0.92（保留底图原色，不做重度降饱和）
   - 对比度 1.06
   - 深蓝叠加层 RGB(16,30,54)，混合比例 **0.22**（轻度统一色调，不压死底色）
   - 整体亮度 **0.97**（比 1.0 略暗，突出白字标题）
   - 底部 72% 以下轻压暗（给署名/日期区留位），暗角极轻
   - **不加装饰线/饰点**——用户明确否掉金色分隔线，画面只留底图+标题
2. 核心原则：**背景服务于标题，不抢标题**；宁可让底图原色透出来，也不要压成一团黑。
3. 输出物：背景合成图 + 上述参数记录（供同系列页面复用）。

### Stage E：精调循环

1. 将标题 PNG 置入背景，测试位置与书风组合。**实测定位（2026-08 用户两轮定稿）**：标题宽约画面 60%，垂直中心在 **26% 高度（中上方偏上）**——用户先要求标题整体位于画面中上部（32%），后又明确"还可以再靠上一点"（26%），给画面主体（帆船/山峰/光）留下方呼吸空间。脚本默认参数已同步。
2. **组合交付硬规则（2026-08 用户定稿）**：**3 种书风 × 2 张底图 = 6 张完整视觉**，全组合出图、HTML 画廊一次交付。每张标注「书风 × 底图」来源，方便用户定位偏好。不做中间挑选——用户直接在 6 张成品里挑。
3. 迭代变量：标题尺寸（60% / 68% 宽）、书风、底图方向；用户挑中方向后再微调参数出 2-3 个精修变体。
4. 每次定稿前判断标准：
   - 高管可读性（3 米外能看清标题）
   - 高端视觉节奏（克制、不堆砌）
   - 单视觉焦点（画面只讲一件事）
5. **交付格式（重要）**：多候选一律用 **HTML 画廊页内嵌 base64 图片**交付——直接逐张推送 PNG 时预览面板可能只显示单素材，用户会误以为"文字和图没合成"。
6. 输出物：6 张组合视觉 + 用户挑中后的精修定稿 + 关键参数备忘（供同系列页面复用）。

## 风格规则（硬性约束）

- **视觉基调**：高端、克制、高级感、强对比、少杂物。
- **配色**：深色底 + 低饱和暖/冷色叠加；避免过亮、霓虹、荧光色。
- **标题层级**：书法字要有意图感，不得与背景"打架"（对比不足或位置冲突均不可）。
- **构图**：保留留白；不对称构图须刻意设计；全图只有一个视觉焦点。
- **美学循环**：定稿前必须测试 2-3 个变体，不得一次成型直接交付。

## 使用方式

1. 用户提出封面/底版需求时，先确认：标题文案、场合（大会/汇报/对外）、期望情绪。
2. 按 Stage 0→E 顺序执行；**标准模式一次跑完**（3 书风 × 2 底图 → 6 张组合画廊交付），用户仅在明确要求时才逐阶段确认。
3. 复杂决策参考 `references/style-checklist.md` 中的清单与配方（含实测定稿调色参数）。
4. 合成与交付优先用 `scripts/` 工具链（见下），不要每次现写脚本。
5. 交付前对照风格规则逐条自检（可读性/配色/构图/焦点/字形正确性）。

## 资源说明

### scripts/（工具链，已实战回归验证）
- `keyout_title.py`：AI 书法黑底白字 → 透明 PNG（亮度抠图）。
- `compose_cover.py`：底图调色 + 标题合成。**默认参数 = 实测定稿配方**，一条命令复现成品：
  `python -X utf8 compose_cover.py --bg 底图.png --title 标题.png --out 成品.png`
- `build_gallery.py`：多张 PNG → HTML 画廊（base64 内嵌），多候选预览的标准交付格式。
- 依赖 Pillow，Windows 中文路径需加 `-X utf8`。

### assets/calligraphy-ref/（书法风格参考库）
用户提供的 7 张小红书书法字体参考图，按风格归类为 5 种：
1. `1-wild-bangshu.jpg` — 狂野榜书（张狂、颠覆、霸气）
2. `2-oriental-poetic.jpg` — 东方诗意（古朴、底蕴、传承）
3. `3-fresh-art*.jpg` — 清新文艺（创新、飘逸、年轻化）
4. `4-design-hand.jpg` — 设计手写（灵动、设计感、现代）
5. `5-cursive-bold.jpg` — 狂草奔放（激情、爆发力、冲刺）

AI 自动选书风时，以 `references/calligraphy-style-ref.md` 的匹配矩阵为决策依据，同时参照上述视觉基准图对齐审美。

### assets/backgrounds/（底图素材库，20 张，分 6 类——仅创意参考）
- `peak/`：高峰/突破主题（3 张）——日照金山、雪山光流、团队攀登
- `tech/`：科技/未来/数据主题（8 张）——光门、数据波浪、AI 芯片、地球夜景等
- `aviation/`：航空/硬核主题（2 张）——战机海面、物流全息
- `industrial/`：工业/制造主题（1 张）——绿色工厂
- `nature/`：自然/生态主题（5 张）——森林、湖泊、公路（偏绿调，常规汇报需强调色）
- `speed/`：速度/光轨主题（1 张）——公路长曝光
- **使用规则（2026-08 用户定稿）**：素材来自网络收集（小红书/花瓣），**仅作创意方向参考，不得直接作为底图输出**（版权原因，本 skill 面向 GitHub 公开分发）。
- **公开打包时必须排除本目录**（package 发布版不含库内底图）；完整索引见 `references/background-library-ref.md`。

### assets/examples/
5 张真实交付案例（1.png - 5.png），作为整体风格基准图。开工前先浏览，对齐用户审美基线。

### references/
- `style-checklist.md`：标题选择清单、底图筛选表、蒙版配方（含 2026-08 实测定稿参数）、配色参考、提示词模板（含正反例）。
- `calligraphy-style-ref.md`：5 种书法风格详细参考库（视觉特征、适用场景、匹配矩阵、生图提示词模板）。
- `background-library-ref.md`：20 张底图素材完整索引（每图详细描述、情绪、颜色基调、契合度评分、使用建议、检索关键词）+ 快速检索矩阵。

## 边界

- **包含**：书法标题预处理、底图筛选与精修、蒙版校色、封面/金句页布局调优（含主副标题层级：书法大字主句 + 衬句小字）、高层汇报视觉基调。
- **不包含**：全自动整页 PPT 生成、通用商务模板、非演示类平面设计。
