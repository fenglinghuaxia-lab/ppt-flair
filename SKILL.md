---
name: ppt-flair
description: 高层汇报/大会视觉封面设计工作流。当用户需要"PPT汇报风格"、"总经理大会封面"、"书法字标题"、"高层汇报底版"、"蒙版设计"、"领导汇报视觉"、"大会汇报封面"、"高级商务视觉"等高管级演示视觉时使用。核心流程五阶段：书法字标题生成(即梦)→气质底图筛选(花瓣)→底图精修(海螺P图/PS)→背景蒙版系统→精调循环。风格基准：深色底+低饱和叠加+书法大字+强对比+单视觉焦点。内置风格清单与配色/蒙版配方。
agent_created: true
---

# PPT Executive Visual（高层汇报视觉风格）

## Overview

为高层汇报、总经理大会封面、商务演讲封面提供一套可复现的高端视觉设计工作流。核心理念是"书法字 + 气质底图 + 蒙版校色"三层结构，以深色基调营造克制、高级、强对比的视觉语言。本 skill 是风格系统而非通用 PPT 教程——所有阶段均围绕"高管可读性 + 高端视觉节奏"这一标准执行。

风格基准可参考 `assets/examples/` 下 5 张真实交付案例图。

## 触发词

- "PPT汇报风格" / "高层汇报PPT风格"
- "总经理大会封面" / "大会汇报封面"
- "书法字标题" / "领导汇报视觉"
- "高层汇报底版" / "蒙版设计"
- 任何要求"高端/大气/商务严肃"的演示封面需求

## 工作流（5 阶段）

### Stage A：标题字体概念

1. 确定标题文案与情绪基调（如"突破"、"远航"、"共创"），每个标题先给 2-3 个书法笔触方向。
2. **首选路径（已实测跑通）**：用内置 AI 生图生成书法字——提示词模板："纯黑色背景上的白色大字「XX」，浓墨行楷/现代书风，笔画遒劲带飞白枯笔，横排居中，无其他文字无水印"，生成 2-3 个书风候选。
3. **亮度抠图**：AI 出图为黑底白字，用 Python PIL 按亮度转透明 PNG（白字保留、黑底剔除），脚本见 `scripts/` 或按此逻辑现写。
4. **错字检查是硬关卡**：AI 生成中文书法有错字风险，透明 PNG 合成前必须让用户确认每个字的字形，有错字就换提示词重生成（一般 1-2 次内命中）。
5. **备选路径**：系统字体库直接渲染（华文行楷/新魏/琥珀），零错字但笔画"印刷感"平，质感明显不如 AI 生图，仅在用户拒绝 AI 路径时使用。
6. 输出物：透明 PNG 标题集 + 每个标题的情绪说明。

### Stage B：底图筛选

1. **首选路径（已实测跑通）**：用内置 AI 生图直接生成底图，提示词关键词：电影级/史诗级、大气恢弘、光占大面积画面、画面下部保留暗部留白给标题、低饱和、无文字无水印。每个方向（如突破/光/海）各出一张。
2. **重要教训：避免"过度压抑"**——纯暗调小光比会被用户否掉；要"大气"就得拉开尺度：暖金晨光做主光、深蓝做基底、云海/山脉撑开空间感。AI 出图可反复重生成，比花瓣检索更可控。
3. 传统路径：花瓣或自建视觉库检索"有故事感"的场景（攀登、航行、破浪、云层、远山等隐喻意象），仅当用户明确要求真实摄影素材时使用。
4. 按以下标准逐项打分，四项全过才入选：
   - 氛围匹配（与标题情绪一致）
   - 构图干净（留白充足，主体不顶格）
   - 对比度强（明暗层次分明）
   - 低噪点（无杂乱元素干扰标题区域）
5. 输出物：候选图 3 张 + 入选理由。

### Stage C：底图精修

1. **首选路径（已实测跑通）**：用 Python PIL 脚本做裁剪与调色，全流程可复现、参数可记录。
2. 裁剪：按 1920×1080（16:9）中心裁切，PPT 全幅直接用。
3. 若底图需要多张合成（如天空+山体），先统一色温再叠加。
4. 输出物：1 张精修主图（高清，预留标题区）。

### Stage D：背景系统搭建

1. **实测定稿配方（2026-08，用户逐轮确认，直接复用）**：
   - 饱和度 0.92（保留底图原色，不做重度降饱和）
   - 对比度 1.06
   - 深蓝叠加层 RGB(16,30,54)，混合比例 **0.22**（轻度统一色调，不压死底色）
   - 整体亮度 **0.97**（比 1.0 略暗，突出白字标题）
   - 底部 72% 以下轻压暗（给署名/日期区留位），暗角极轻
   - **不加装饰线/饰点**——用户明确否掉金色分隔线，画面只留底图+标题
2. 核心原则：**背景服务于标题，不抢标题**；宁可让底图原色透出来，也不要压成一团黑。
3. 输出物：背景合成图 + 上述参数记录（供同系列页面复用）。

### Stage E：精调循环

1. 将标题 PNG 置入背景，测试位置与书风组合。**实测定位**：标题宽约画面 60%，垂直中心在 **40% 高度（中线以上）**——用户明确否掉中线以下的位置。
2. 迭代变量：标题尺寸（60% / 68% 宽）、书风（浓墨榜书 vs 锐利现代）、位置。
3. 每次定稿前至少测试 2-3 个版本，判断标准：
   - 高管可读性（3 米外能看清标题）
   - 高端视觉节奏（克制、不堆砌）
   - 单视觉焦点（画面只讲一件事）
4. **交付格式（重要）**：多候选一律用 **HTML 画廊页内嵌 base64 图片**交付——直接逐张推送 PNG 时预览面板可能只显示单素材，用户会误以为"文字和图没合成"。
5. 推荐节奏：逐阶段与用户确认（A 出标题候选→确认字形与书风，B 出底图→确认画面情绪，C+D 一次跑完出完整封面，E 出 2-3 变体选稿），每阶段一个确认点。
6. 输出物：最终封面图 + 关键参数备忘（供同系列页面复用）。

## 风格规则（硬性约束）

- **视觉基调**：高端、克制、高级感、强对比、少杂物。
- **配色**：深色底 + 低饱和暖/冷色叠加；避免过亮、霓虹、荧光色。
- **标题层级**：书法字要有意图感，不得与背景"打架"（对比不足或位置冲突均不可）。
- **构图**：保留留白；不对称构图须刻意设计；全图只有一个视觉焦点。
- **美学循环**：定稿前必须测试 2-3 个变体，不得一次成型直接交付。

## 使用方式

1. 用户提出封面/底版需求时，先确认：标题文案、场合（大会/汇报/对外）、期望情绪。
2. 按 Stage A→E 顺序执行，每阶段结束向用户确认方向后再进入下一阶段。
3. 复杂决策参考 `references/style-checklist.md` 中的清单与配方（含实测定稿调色参数）。
4. 合成与交付优先用 `scripts/` 工具链（见下），不要每次现写脚本。
5. 交付前对照风格规则逐条自检（可读性/配色/构图/焦点/字形正确性）。

## 资源说明

### scripts/（工具链，已实战回归验证）
- `keyout_title.py`：AI 书法黑底白字 → 透明 PNG（亮度抠图）。
- `compose_cover.py`：底图调色 + 标题合成。**默认参数 = 实测定稿配方**，一条命令复现成品：
  `python -X utf8 compose_cover.py --bg 底图.png --title 标题.png --out 成品.png`
- `build_gallery.py`：多张 PNG → HTML 画廊（base64 内嵌），多候选预览的标准交付格式。
- 依赖 Pillow，Windows 中文路径需加 `-X utf8`。

### assets/examples/
5 张真实交付案例（1.png - 5.png），作为风格基准图。开工前先浏览，对齐用户审美基线。

### references/
- `style-checklist.md`：标题选择清单、底图筛选表、蒙版配方（含 2026-08 实测定稿参数）、配色参考、提示词模板（含正反例）。

## 边界

- **包含**：书法标题预处理、底图筛选与精修、蒙版校色、封面布局调优、高层汇报视觉基调。
- **不包含**：全自动整页 PPT 生成、通用商务模板、非演示类平面设计。
